#!/usr/bin/env bash

set -euo pipefail

echo "Installing tools..."

brew install --cask ghostty syncthing obsidian raycast cameracontroller
brew install git git-delta ripgrep jq fzf k9s sqlc figlet bat ranger curl lazygit tailscale tmux starship tree protobuf

echo "✅ Tools installation completed"
exit 0
